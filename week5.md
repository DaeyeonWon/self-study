---
layout: default
title: 5주차. Vector Databases & Retrieval Architecture Design
---

# 5주차: Vector Databases & Retrieval Architecture Design (벡터 데이터베이스 및 검색 아키텍처 설계)

지난 4주 차 기술을 통해 모든 단어와 문맥들이 수학적 의미가 담긴 '벡터 차원의 숫자 배열'로 무수히 변환되었습니다. 수많은 데이터를 전부 좌표상에 뿌려놓았다면, 사용자가 질문을 던지는 실시간 환경(0.1초 이내)에서 가장 가까운 문서를 찾아내려면 어떤 인프라가 필요할까요?
이 천문학적인 수학 연산을 소화할 수 있는 인프라, **벡터 데이터베이스(Vector Database)** 의 원리와 실제 데이터 파이프라인의 설계에 대해 알아보겠습니다.

---

## 1. 벡터 데이터베이스(Vector Database)란 무엇인가?

오라클, MySQL과 같이 우리가 흔히 아는 기존 정형 데이터베이스(RDB)들은 행(Row)과 열(Column)이라는 엑셀 표와 같은 테이블 구조에 의존해서 데이터를 깔끔하게 담습니다. 따라서 "가격이 5만원 이상이고 이름이 사과인 정보를 가져와"라는 스키마 질의는 할 수 있지만, "모호하게 뭔가 슬프면서 따뜻한 분위기의 문장을 가져와"라는 의미론적 질의는 아예 불가능합니다.

반면, **벡터 데이터베이스(Vector Database)** 는 데이터베이스 자체 공간의 패러다임이 다릅니다. 데이터를 고정된 테이블 열이 아니라 수천 차원 축소를 가진 좌표계 상의 공간 지점으로 저장합니다. 

![Vector DB Layout](assets/images/page_40_img_3093.png)
*(PDF 자료 참고: 벡터 인덱스 검색 아키텍처와 DB 통합 형태)*

> 🗄️ **이해를 돕는 예시: 이름표 붙은 사물함 vs 체육관에 흩어진 공**
> 전통적인 RDB는 우체국처럼 'A열 3번 사물함'이라는 고정된 주소가 있어야만 소포를 꺼내올 수 있습니다. (정확히 일치하는 문자열을 조회해야 함)
> 반면에 벡터 DB는 축구장만 한 거대한 체육관 바닥입니다. 의미가 비슷한 농구공끼리, 야구공끼리 뭉쳐 다니며 체육관 곳곳에 어지럽게(자유 공간에) 퍼져 있습니다. 관리자인 당신이 "축구공과 비슷한 재질의 공" 이라는 목표물(질문)을 던지면, 0.1초 만에 체육관 전체의 거리를 계산해 목표물에서 반경 1m 이내에 옹기종기 모여있는 비슷한 구기 종목 공들의 그룹 뭉텅이를 통째로 뜰채로 잡아채오는 방식으로 데이터를 로드합니다.

## 2. ANN(Approximate Nearest Neighbor): 초고속 유사도 탐색 엔진의 심장

수억 개의 문장을 인덱싱해둔 데이터베이스 상에서 원칙적으로 가장 정확한 문서를 찾으려면 1번 문서부터 1억 번째 문서까지 일일이 코사인 유사도 공식을 다 계산해 봐야 합니다 (이를 100% 완전 탐색 시스템 K-NN이라고 합니다). 하지만 이렇게 계산하면 질문 하나에 시스템이 다운되거나 응답에 수 시간이 걸릴 수 있습니다.

해결책은 정확도를 극소량(예: 0.1%) 양보하는 대신 검색 속도를 수천 배 이상 끌어올리는 극도로 기민한 알고리즘인 **근사 최근접 이웃(ANN, Approximate Nearest Neighbor) 알고리즘** 을 도입하는 것입니다. 여기서 대부분의 벡터 DB 성능이 결정됩니다.

![ANN Tree Structure](assets/images/page_40_img_3124.png)
*(PDF 자료 참고: ANN 탐색 속도를 높여주는 계층 구조 모델)*

### 대표적인 ANN 알고리즘: HNSW (Hierarchical Navigable Small World)
현재 엔터프라이즈 벡터 DB에서 독보적으로 많이 쓰이는 그래프 탐색 논리 알고리즘 기법입니다.
모든 문서들의 점끼리 '친구 관계(간선 거리)' 네트워킹을 먼저 만들어 놓은 뒤, 층(계층)을 여러 겹으로 쌓습니다. 

* 맨 꼭대기 층에는 매우 듬성듬성한 소규모의 대략적인 대장 문서 점들만이 존재합니다.
* 사용자의 검색 표창(질문)이 꼭대기 층에 착륙해서 대장 문서들을 스캔해 방향을 제일 크게 잡고 계단을 통해 아랫층으로 쭉 미끄러지며 내려옵니다 (Zoom-in). 
* 최하단 상세 층에서는 대략적인 위치를 이미 다 파고들었으니, 그 주변 반경에서만 정밀하게 탐색을 마무리합니다.

> ✈️ **이해를 돕는 예시: 비행기 조종석의 하강과 착륙**
> 1억 개의 건물 중, 내 친구 "철수네 집"을 찾는 과정입니다. 
> 1) 상공 10km (상위 계층): 지구 전체를 훑다가 가장 따뜻한 아시아 대륙 한국 지역으로 기수를 크게 돌립니다. 
> 2) 상공 1km (중위 계층): 한국 안에서 경기도라는 시가지로 조밀하게 접근합니다.
> 3) 지상 (최하위 계층): 경기도 땅에 곧바로 하강해 반경 100m 이내만 육안으로 철저히 뒤집어 철수네 집을 찾아냅니다. 
> 이 방식은 일본이나 아프리카 같이 엉뚱한 대륙을 전부 훑어 찾아보는 바보 같은 시간 낭비를 엄청나게 절감해 줍니다!

## 3. 벡터 DB 플랫폼 선택 시 엔터프라이즈의 고려사항

초기 구축 비용 절감을 위한 여러 벡터 데이터베이스 플랫폼이 난립하고 있습니다 (Milvus, Pinecone, Weaviate, Qdrant 등). 시스템 통합 기획 시 봐야 할 핵심 스펙들은 무엇일까요?

1. **Fully Managed Cloud (서버리스) vs On-Premises (자가 서버):** 폐쇄된 사내 전산망에서 기밀문서를 절대 클라우드로 내보낼 수 없는 보안 조건이라면 Pinecone 같은 강력한 SaaS보다 Milvus 등의 자체 망 설치 구축 방식을 무조건 최우선으로 검토해야 합니다.
2. **복합 하이브리드 서치(Hybrid Search) 지원 여부:** 임베딩으로 추상적 의미만 찾는 건 부족할 때가 있습니다. (예: "2023년이라는 정확한 단어가 꼭 들어가야만 돼!"라는 정확성). 이를 위해 예전의 키워드 기술인 BM25 검색과 벡터 유사도 탐색을 동시에 지원하고 통합하는 기능이 필수로 있어야 합니다.
3. **업데이트 지연성(Latency) 및 스케일링(Scale-out):** 매초 데이터가 새로 생성되고 변질되는 실시간 뉴스 봇 시스템의 경우, CRUD(특히 벡터 업데이트/제거) 성능이 치명적으로 중요합니다.

---

## 마무리하며

이번 강좌에서는 방대한 데이터를 단순히 텍스트 쪼가리가 아닌, 영리하게 설계된 차원의 숲(Vector DB)으로 집어넣는 거대한 정보 건축학의 원리를 소화했습니다. 그리고 미사일처럼 가장 비슷한 표적 좌표를 추적해 내는 ANN 검색의 원리를 배웠습니다!

하지만 데이터베이스 시스템이 던져준 탑 랭킹(1등부터 10등) 결과 문서들이 사용자에게 정말로 최고의 의미 배열(Context) 순서라고 그 누가 100% 확신하고 장담할 수 있을까요? 6주차에는 AI 모델을 한 번 더 투입해 검색된 수십 개의 논문을 날카롭게 뒤집어 평가하고 우선순위 재정렬을 매기는 **Reranking Models and Hybrid Retrieval Techniques(리랭킹 모델과 하이브리드 검색론)** 에 대해서 무궁무진한 테크닉을 배워보겠습니다.
