---
layout: default
title: 5주차. Vector Databases & Retrieval Architecture Design
---

# 5주차: Vector Databases & Retrieval Architecture Design (벡터 데이터베이스 및 검색 아키텍처 설계)

지난 4주차까지의 기술을 통해 문서와 지식의 조각들이 엄청난 수학적 의미를 함축한 '천문학적 고차원(예: 1536차원) 숫자 배열(벡터)'들로 치환되었습니다.
모든 문서를 거대한 수치(Vector) 형태로 바꿔 허공의 차원에 흩뿌려 놓았습니다. 서비스 사용자가 RAG 채팅창에 질문을 던질 때 시스템이 버벅거린다면 사용자들은 떠나갈 것입니다. 어떻게 1억 개가 넘는 문장 좌표들 사이에서 0.1초 만에 최적의 위치 정답들을 퍼올릴 수 있을까요? 

이를 위해 탄생한 초고속 수색망 인프라스트럭처의 혁명, **벡터 데이터베이스(Vector Database)** 의 코어 원리와 엔터프라이즈 통합 아키텍처 설계 메커니즘을 심층 분석합니다.

---

## 1. 기존 데이터베이스와의 궤를 달리 하는 벡터 DB (개념과 탄생)

오라클, MySQL, PostgreSQL과 같은 전통적 관계형 테이블 RDB(Relational Database)들은 행(Row)과 열(Column)이라는 격자형 스키마 구조 아래 수납함을 지정해 데이터를 규격화시켜 저장했습니다.
이 모델에서는 `SELECT * FROM table WHERE item='사과'` 와 같은 1차원적인 단순 스펠링 일치 서치는 즉효성을 가지지만, 
"뭔가 약간 슬프지만 끝엔 감동으로 마무리되는 분위기의 청춘 소설 글귀 좀 찾아줘" 식의 기상천외한 시맨틱(의미론적/유사도) 질의는 원천적으로 불가능합니다! 분위기와 뉘앙스는 엑셀 표에 컬럼화 시켜 담을 수 없는 무형의 영역이기 때문이죠.

이 구조적 한계를 부수고 탄생한 **벡터 데이터베이스(Vector Database)** 는 데이터를 엑셀 테이블 서랍장에 넣는 것이 아니라, 수천 차원 공간에 퍼진 X-Y-Z 자유 좌표 속 별들로 데이터를 둥둥 띄워 무질서하게(하지만 내재적으론 군집하여) 저장합니다. 

![Vector DB Architecture](assets/images_new/Fig_5_1_page_136.png)
*Fig 5.1: 전통적 스칼라 DB와 차별화된 다차원 기반 Vector DB의 인덱싱 모델링 구조 모식도.*

> 🗄️ **이해를 돕는 예시: 네임택 붙기 사물함 vs 거대한 도서관 광장에 쌓인 책**
> 전통적인 RDB는 우체국처럼 'A열 3번 사물함'이라는 강제된 주소가 있어야 찾을 수 있습니다.
> 반면에 벡터 DB는 초대형 축구장 바닥에 1억 권의 책들이 표지가 안보이게 자유자재로 널브러져 있는 광장입니다. 다만 마법처럼 판타지 소설끼리, 의학서적끼리 반경 내에 옹기종기 자석처럼 모여 있습니다.
> 관리자는 "축구 서적 비슷한 질감의 책 좀..." 이라는 자석 좌표(쿼리 벡터)를 던지면 바닥 전체 면적에서 즉시 해당 자석 근처 반경 2m 내에 오밀조밀 모인 다른 조각 책들을 한꺼번에 뜰채로 잡아끌어 퍼 올립니다! 엄청난 기동성과 의미론적 연쇄 탐색의 위력을 의미합니다.

---

## 2. ANN (Approximate Nearest Neighbor): 초고도 유사도 탐색 엔진의 두뇌

우리가 1차원의 수학 코사인 각도로 유사한 거리를 찾아낸다고 배웠습니다. 하지만 치명적인 문제가 있습니다. 코사인 공식을 매번 1억 개의 문서 좌표들과 질문 한 개 좌표끼리 전부 1대1 무식하게 일일이 사칙연산을 돌리면(K-NN/브루트포스 완전 탐색), 서버 CPU가 연산 압력에 다운되거나 답변 하나 받는데 20분씩 지연됩니다.

이를 실시간 인메모리 서치(0.01초)로 줄여주는 구원투수 알고리즘이 바로 **근사 최근접 이웃 검색 (ANN, Approximate Nearest Neighbor)** 엔진입니다.
탐색의 '정확도'를 한 0.1% 양보하고 포기하는 대가로, 속도를 수만 배 폭등시키는 야만적이고도 우아한 알고리즘입니다.

![ANN Tree Strategy](assets/images_new/Fig_5_2_page_137.png)
*Fig 5.2: ANN 알고리즘을 통한 공간 및 트리 분할, 계층화 군집을 통해 검색 비용을 급감시키는 원리 모델.*

---

## 🌟 [Deep Dive] 천문학적 벡터 인프라를 가능케 한 전설적 논문 아키텍처

아무리 화살표(벡터)가 뜻을 잘 담아도, 그걸 0.1초 만에 가져와 주지 못하면 AI 서비스는 파멸합니다. 이 불가능의 공간 연산 지연 한계(Latency bottleneck)를 돌파한 미친 수학자 연구진들의 학술적 거대한 도약 발자취를 세밀하게 추적합니다. 

### 📜 1. HNSW: 피라미드식 스키장 슬로프 네트워크 건설
**[논문명]** *Efficient and Robust Approximate Nearest Neighbor Search Using Hierarchical Navigable Small World Graphs (Malkov et al., 2018)*
* **연구 배경:** 기존에는 K-means 군집화로 우주를 갈라 보려고 했지만 여전히 경계선 찾기에 버벅거리며 시간이 너무 오래 걸렸습니다.
* **해결 기술 (Architecture):** 
  현재 세상 거의 모든 상업용 벡터 DB(Milvus, Pinecone, Qdrant 등)가 무조건 차용하는 궁극의 최강 탐색 노드 분할 그래프입니다. 문서 점들 간에 친구 관계망을 짜는데, 이 층상 구조도를 '스키 슬로프'처럼 여러 위상 층으로 고도화하여 계단으로 설계합니다.
  - 최상단 피라미드 꼭대기 층에는 매우 듬성듬성한 거대 바운더리 '알파 대장급 대표 문서' 벡터 점들만 살아 있습니다.
  - 꼭대기에 사용자의 질문 표창이 착륙하면 대장 문서들과 거리를 비교해 방향과 각을 크게 훑어 냅니다 (Zoom-In).
  - 방향을 잡자마자 밑 층으로 쭉 미끄러져 내려와 탐색을 연계하고, 최하위 세부 층에 와서는 이미 정답 구역 반경 100m로 좁혀진 상태라 거기서만 고속 돋보기 훑기를 완료해 도출합니다.
* **의의:** 기존 트리 기반의 검색 복잡도를 기적의 시간 복잡도 `O(log N)` 으로 급강하 시킨 역사적 마일스톤입니다!

<div class="mermaid">
graph TD
    subgraph Layer 2 (개조감도 - 노드 매우 희소)
    L2_A(대장 문서 A) --- L2_B(대장 문서 B)
    Q((쿼리 점 착륙!)) -.->|가장 가까운 B로| L2_B
    end
    subgraph Layer 1 (중간 시야)
    L1_B(대표 문서 B) --- L1_C(문서 C)
    L2_B ==>|층 하강 깊숙이 파고들기| L1_B
    L1_B -.-> L1_C
    end
    subgraph Layer 0 (최하단 전수색 - 모든 노드 밀집)
    L0_C(문서 C) --- L0_Target(정답 D!) --- L0_E(문서 E)
    L1_C ==>|층 하강 좁은 반경 도달| L0_C
    L0_C -.-> L0_Target
    end
    style Q fill:#ffcccc,stroke:#ff0000,stroke-width:2px
    style L0_Target fill:#cce5ff,stroke:#0000ff,stroke-width:3px
</div>

### 📜 2. FAISS: 10억 단위(Billion-scale) 매머드급 GPU 벡터 연산 폭주 기관차 
**[논문명]** *Billion-scale similarity search with GPUs (Johnson et al., Facebook AI Research / Meta, 2019)*
* **연구 배경:** CPU 메모리(RAM) 위에서 HNSW 트리 구조 그래프를 짜고 노는 것도 좋지만, 글로벌 페이스북 10억 명의 데이터, 문서 단계를 넘어 이미지 텐서 덩어리 같은 상상을 초월하는 메모리 압박이 오자 디스크 로드가 감당치를 초과했습니다.
* **해결 기술 (Architecture):**
  메타 연구소는 연산의 병렬 처리 특공대원인 GPU 그래픽 카드를 아예 벡터 탐색 연산에 직접 다이렉트로 할당 갈아 넣는 최상위 무기 프레임워크인 **Faiss 라이브러리**를 창조 오픈소스로 풀었습니다. 
  특히 내부적인 기법으로 **IVF-ADC (Inverted File System with Asymmetric Distance Computation)**를 도입, 우주 공간을 수만 개의 셀(Cell) 격자로 찢어버려 인덱스를 세우고, 벡터들의 기나긴 고차원 소수점 숫자 무리들을 양자화(Quantization) 시켜 통쨰로 찌그러뜨려 파일 압축 사이즈를 10분의 1로 극소화 시켜 메모리에 거대하게 적재 상주시켰습니다.
* **의의:** HNSW가 "찾아가는 그래프 지도의 예술"이라면, FAISS는 10억 개의 데이터도 GPU 메모리 폭주 병렬연산의 힘을 빌려 양자화 압축 파워로 뭉개 거대한 스케일 몬스터 처리량을 감당할 때 사용하는 절대 우위 포지션을 가져갔습니다.

<div class="mermaid">
stateDiagram-v2
    [*] --> 거대_벡터_배열_입력
    거대_벡터_배열_입력 --> IVF_클러스터_분류: 우주 공간을 수천 개 블록 지역망 셀로 칼질
    IVF_클러스터_분류 --> PQ_양자화_압축: 1536차원의 방대한 크기를 \n 8비트급 용량으로 찌그러뜨려 RAM 거치
    PQ_양자화_압축 --> GPU_병렬_스캐닝: GPU 코어 수천 개 동시 가동
    GPU_병렬_스캐닝 --> 빛의속도_결과출력
</div>

### 📜 3. Milvus: 클라우드 파편화 분산 처리의 결정체 아키텍처
**[아키텍처 스탠다드]** *Milvus: A Purpose-Built Vector Data Management System (Wang et al., ACM SIGMOD 2021)*
* **연구 배경:** 앞선 알고리즘 툴들은 컴퓨터 '노드 1대' 안에서의 싸움이었습니다. 빅테크 서버실에서 컴퓨터 수백 대 클러스터 네트워크로 묶어 무거운 트래픽을 분산 방어하는 진정한 클라우드 네이티브형 독립적 데이터베이스 통합 체인이 절실했습니다.
* **해결 기술 (Architecture):**
  검색, 쿼리 파싱 노드와, 실제 데이터가 저장되는 저장 스토리지 노드를 원천적으로 분리 이산하여(Micro-service 분리 아키텍처의 혁명) 대규모 확장에 대비했습니다. 방대한 양 벡터가 쌓일 때 스무스 트래픽 조절로 서버를 1대, 10대, 100대로 옆으로 줄 세워서 붙여 확장(Scale-out)할 수 있는 완벽한 안정성 클러스터 생태계 톱니바퀴 샤딩 파이프라인 정점을 보였습니다. 

---

## 마무리하며

오늘은 수많은 텍스트들을 단순 좌표로 저장하는 데서 멈추지 않고, 엄청난 지연 시간을 단축하여 기계만이 지니는 초정밀 수색망 데이터베이스(Vector DB) 및 ANN 로직으로 벼려내는 스케일을 탐험했습니다! 거대한 지식 창고를 설계한 셈입니다.

하지만 여기서 끝이 아닙니다. 아무리 벡터 공간 안에서 가장 화살표 방향이 유사한 1, 2, 3위 문서 문단을 퍼올려 주었다 하더라도, 그 서열 등수가 우리가 직관적으로 납득할 만한 *가장 치명적이고 알짜 정답으로 순위를 내린 문서가 진짜로 맞을까요?* LLM 컨텍스트 공간은 제한이 있으므로 정답은 무조건 최정예 1~5등으로 도출되어 프롬프트에 들어가야만 합니다 안 그러면 밀려나오거나 무시당합니다. 

이를 해결하기 위해 인공지능이 1차 검색 결과를 전부 다시 테이블 위에 깔고 일일이 채점관을 돌려 재심사(Re-sort)하여 진정한 고도 순위만을 골라내는 궁극의 등용문 파이프라인! 6주차 강좌, **Reranking Models and Hybrid Retrieval Techniques(리랭커 모델과 하이브리드 검색론 기법)** 에서 그 엄청난 성적표 재조작 기술의 기적을 체험해 보겠습니다.
