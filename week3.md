---
layout: default
title: 3주차. Advanced Document Chunking & Context Engineering
---

# 3주차: Advanced Document Chunking & Context Engineering (고급 문서 청킹과 문맥 엔지니어링)

RAG 시스템에서 데이터를 아무리 잘 가져와도, 잘게 쪼개는 방식이 잘못되면 LLM이 내용을 전혀 이해하지 못하여 1주차에서 확인한 수많은 챌린지들에 그대로 노출됩니다. 3주차에서는 방대한 데이터를 LLM이 쉽게 소화할 수 있도록 문서를 나누는 **'청킹(Chunking)'** 기술과 데이터 더미에 문맥의 품질을 극대화시켜 입혀주는 방법에 대해 배웁니다.

---

## 1. 청킹(Chunking)이란 무엇이며, 왜 핵심인가?

RAG 시스템을 구축할 때 PDF나 수천 페이지짜리 기술 매뉴얼을 LLM에게 통째로 던져줄 수 없습니다. 모든 언어 모델에는 **컨텍스트 윈도우(Context Window) / 토큰 한계(Token Limit)** 라는 한 번에 읽고 이해할 수 있는 정보의 상한선이 정해져 있기 때문입니다. 수많은 데이터를 통으로 넣게 되면 컴퓨팅 비용은 기하급수적으로 폭증하고, LLM은 앞선 내용을 잊어버리는 등 처리 속도 최적화가 완벽히 붕괴됩니다.

따라서 긴 문서를 적절하고 **최적의 크기 단위를 가진 텍스트 덩어리(Chunk)들로 절단하여 데이터베이스에 분류 수납**해야 합니다.

> 🍔 **이해를 돕는 예시: 커다란 수박 썰기**
> 커다란 수박(긴 문서)을 한 통 그대로 입(LLM)에 욱여넣을 수는 없습니다. 믹서기 입구에 맞춰 먹기 좋은 크기(Chunk)로 깍둑썰기를 해야만 들어갑니다. 하지만 수박을 무지성으로 막 썰어서 껍질과 씨앗만 들어가는 퍼즐이 생긴다면? LLM은 뱉어버릴 것입니다. 의미가 끊어지지 않고, 가장 맛있고 온전한 과육의 형태로 썰어주는 기술이 바로 잘 설계된 스마트 청킹입니다.

![Document Chunking Mechanics](assets/images/page_20_img_1829.png)
*(PDF 자료 참고: 청킹의 알고리즘 메커니즘과 벡터 분리)*

---

## 2. 청킹의 세 가지 발전 단계

청킹 기법은 기술 패러다임에 따라 가장 단순한 방식부터 의미 구조를 파악하는 고난도 방식까지 크게 3단계로 발전해 왔습니다.

### 2.1 고정 크기 청킹 (Fixed-size / Character-based Chunking)
가장 초보적이고 보편적으로 쓰이는 레거시 방식입니다. 그냥 글자 수(예: 300자)나 토큰 수에 맞춰 가위로 딱 자르듯 잘라냅니다. 

* **장점:** 알고리즘 구현이 매우 쉽고, 컴퓨팅 연산이 0에 가깝습니다. 데이터베이스 구축이 일진월보로 빠릅니다.
* **단점:** 의미의 문맥이 철저히 무시된 채 중간에 단어가 반으로 잘리는 위험이 매우 큽니다. 검색 성능을 떨어뜨리는 가장 큰 주범입니다.

> 📝 **이해를 돕는 예시:**
> 글자 수를 10글자로 자른다고 가정해 봅시다. 원본 문장이 "사과는 맛있다. 그런데 바나나는 껍질을 까야 한다." 일 때,
> * 1번 청크: "사과는 맛있다. 그"
> * 2번 청크: "런데 바나나는 껍질"
> 이런 식으로 문장과 의미가 중간에 댕강 잘려버려서, "바나나"를 검색했는데 앞에 "그런데"의 단서를 놓치게 됩니다.

### 2.2 오버랩 청킹 (Overlapping Technique)
고정 크기 청킹의 치명적인 "문맥이 잘리는 단점"을 응급처치하고자 고안된 기법입니다. 앞뒤 청크들이 서로 일정 부분의 문장을 겹쳐서(Overlap) 꼬리표로 물리도록 만드는 것입니다.

> 겹치는 길이 영역(예: 3글자 오버랩)을 설정하면 아래와 같이 유연하게 잘립니다.
> * 1번 청크: "사과는 맛있다. 그"
> * 2번 청크: "(다. 그)런데 바나나"
> 
> 🔗 **이해를 돕는 예시: 파노라마 사진 촬영**
> 스마트폰으로 멋진 풍경의 파노라마를 찍을 때, 사진이 어긋나지 않게 도화지처럼 부드럽게 이어지도록 가장자리를 조금 겹치게 구도를 잡습니다. 오버랩도 이처럼 텍스트 맥락의 끊김을 방지하는 스마트한 이음새 안전망 역할을 합니다. 

### 2.3 의미론적 청킹 (Semantic / Document Structure Chunking)
단순한 형태(길이)가 아닌 문장의 "구조와 의미 단위"로 나누는 가장 진보된 RAG 청킹 방식입니다. 
마침표나 문단 구분, 또는 마크다운(Markdown) 문서의 헤딩(`##`, `###`) 구조와 HTML의 태그를 기준으로 의미 덩어리를 뜯어냅니다. 

> 📚 **이해를 돕는 예시: 역사책 복사하기**
> 교과서 1페이지 하단에서 "제1장 조선 전기" 내용이 시작된다면, 남은 여백이 있다고 기계적으로 500자씩 자르는 것이 아니라 챕터(의미 단위)에 맞춰서 한 번에 분절하여 보관하는 것입니다.

최근에는 기계학습 모델들이 문장마다 의미 유사성(Similarity) 차이를 수학적으로 계산해서, '스토리(주제)가 전환되는 구간'을 기가 막히게 감지해 자동으로 청크의 길이를 동적으로 조절하며 자르는 인지적 청킹 솔루션들까지 엔터프라이즈 환경에 적용되고 있습니다.

---

## 3. 문맥 엔지니어링 (Context Engineering)

![Context Quality Process](assets/images/page_20_img_1835.png)
*(PDF 자료 참고: 메타데이터가 붙은 컨텍스트 엔지니어링 구조)*

아주 잘 썰어낸 텍스트 청크 조각들은 수만 개로 쪼개져 데이터베이스에 흩뿌려집니다. 그 상태 자체로는 원본 문서 안에서 어떤 위치에 속했는지를 잃어버리는 "구조적 기억상실증"에 걸릴 우려가 높습니다. 

이 부작용을 막기 위해 쪼개진 청크들 옆에 다양한 정보의 꼬리표를 같이 엮어주는 과정을 문맥 엔지니어링이라고 합니다. 

1. **메타데이터(Metadata) 부착 규칙:** 
   각 쪼개진 청크들의 속성에 원본(출처) 파일명, 작성 일자, 저자, 카테고리(예: 제품 매뉴얼, 인사 규정 등), 속한 챕터 이름 등을 태그 목록으로 명시해 달아줍니다. 향후 "2023년 제품 매뉴얼 안에서만 관련 정보를 줘" 처럼 메타데이터 필터링을 걸기에 매우 필수적입니다.
   
2. **요약 부착 기법 (Summary-Augmented Chunking):** 
   각 챕터나 문서를 청킹하기 전에 LLM을 통해 "문서 전체 요약본"을 미리 뽑아냅니다. 그리고 수없이 쪼개진 각각의 파편들 앞에 미리 만들어 둔 전체 요약본을 도장처럼 찍어 모두 덧붙여줍니다. 파편화된 조각만 봐도 숲 전체의 맥락을 간직할 수 있어 검색 품질이 비약적으로 좋아집니다.

> 🗂️ **이해를 돕는 예시: 도서관 서가를 뜯어 낱장 보관하기**
> 도서관의 백과사전들을 뜯어 책장 단위의 낱장(Chunk)으로 흩뜨려서 박스에 담는다고 해봅시다. 낱장(파편)만 보면 이 페이지 내용이 무슨 책의 전체 내용인지 알 길이 없습니다. 
> 메타데이터를 추가한다는 것은 모든 낱장의 상단 모서리에 `[태조 이성계 위인전 - 제 3장 건국 - 24p]` 라는 포스트잇(Metadata 태그)을 붙여두고, 추가로 요약본인 `[이 책은 조선 건국 과정의 서적임]` 이란 메모를 달아두는 것과 같습니다. 이 세심한 라벨링 정보가 검색관(DB엔진)에게는 결정적인 추론 단서가 됩니다. 

---

## 마무리하며

오늘은 데이터를 단순히 스크래핑해서 박스에 밀어 넣지 않고 LLM이 절대 소화 불량에 걸리지 않도록 맛있고 안전한 형태로 가공하는 고급 청킹 구조 기술을 분석했습니다. RAG의 성능 격차는 모델 자체보다 여기서 더 격하게 벌어진다고 해도 과언이 아닙니다.

이제 잘게 자르는 것은 끝났습니다. 다음 4주차에서는 이렇게 잘린 알파벳(문자열) 덩어리들을 컴퓨터의 언어인 숫자로 이해시키는 기술, 즉 어떻게 수학적인 연관성 잣대를 들이대 서로 비슷한 내용을 찾아갈 수 있는지 보여주는 **임베딩(Embedding) 모델** 과 **표현 학습(Representation Learning)** 이라는 아름다운 데이터 좌표계에 대해 집중 탐구해보겠습니다.
