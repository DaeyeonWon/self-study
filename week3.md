---
layout: default
title: 3주차. Advanced Document Chunking & Context Engineering
---

# 3주차: Advanced Document Chunking & Context Engineering (고급 문서 청킹과 문맥 엔지니어링)

RAG 시스템에서 데이터를 아무리 잘 가져와도, 잘게 쪼개는 방식이 잘못되면 LLM이 내용을 전혀 이해하지 못합니다. 3주차에서는 방대한 데이터를 LLM이 쉽게 소화할 수 있도록 문서를 나누는 **'청킹(Chunking)'** 기술과 문맥 퀄리티를 끌어올리는 방법에 대해 배웁니다.

---

## 1. 청킹(Chunking)이란 무엇인가?

RAG 시스템을 구축할 때 PDF나 수백 페이지짜리 매뉴얼을 LLM에게 통째로 던져줄 수는 없습니다. 모델마다 한 번에 처리할 수 있는 정보의 양(Token Limit)이 정해져 있고, 너무 많은 데이터를 넣으면 비용이 기하급수적으로 늘고 처리 속도가 느려지기 때문입니다. 그래서 **긴 문서를 적절한 크기의 덩어리(Chunk)로 잘라주어야 하는데, 이 과정을 청킹(Chunking)** 이라고 합니다.

> 🍔 **이해를 돕는 예시:** 
> 커다란 수박(긴 문서)을 한 통 그대로 입(LLM)에 넣을 수는 없습니다. 먹기 좋은 크기(Chunk)로 깍둑썰기를 해야만 소화할 수 있습니다. 하지만 수박을 아무렇게나 막 썰어서 껍질과 씨만 있는 덩어리가 생긴다면 먹기 불편할 것입니다. 가장 맛있고 온전한 과육의 형태로 썰어주는 기술이 바로 잘 설계된 청킹입니다.

---

## 2. 청킹의 종류와 발전된 형태

### 2.1 고정 크기 청킹 (Fixed-size Chunking)
가장 단순하고 많이 쓰이는 방법입니다. 단순히 글자 수 단위(예: 500자)나 토큰 단위로 기계적으로 싹둑 자릅니다.
* **장점:** 구현이 매우 쉽고 컴퓨팅 연산이 빠릅니다.
* **단점:** 문맥이 중간에 잘릴 위험이 매우 큽니다.

> 📝 **이해를 돕는 예시:**
> 글자 수를 10글자로 자른다고 가정해 봅시다. 원본 문장이 "사과는 맛있다. 그런데 바나나는 껍질을 까야 한다." 일 때,
> * 1번 청크: "사과는 맛있다. 그"
> * 2번 청크: "런데 바나나는 껍질"
> 이런 식으로 문장과 의미가 중간에 댕강 잘려버려서 이후 검색 과정 시 모델이 제대로 문맥을 파악하지 못하게 됩니다.

### 2.2 오버랩 (Overlapping) 주기
고정 크기 청킹의 단점을 보완하고자, 앞뒤 청크들이 일정 부분의 내용을 겹쳐서 공유하도록 만드는 기법입니다.

> 겹치는 영역을 설정하면 다음과 같이 잘립니다 (오버랩 3글자 적용).
> * 1번 청크: "사과는 맛있다. 그"
> * 2번 청크: "(다. 그)런데 바나나"
> 
> 🔗 **이해를 돕는 예시:**
> 파노라마 사진을 찍을 때를 상상해 보세요. 풍경이 어긋나지 않고 부드럽게 이어지도록 한 장을 찍고 그 다음 장을 찍을 때 이전 풍경의 가장자리를 조금 겹치게 구도를 잡습니다. 오버랩도 이처럼 텍스트 맥락의 끊김을 방지하는 안전망 역할을 합니다.

### 2.3 의미 기반 / 의미론적 청킹 (Semantic Chunking)
단순한 형태(길이)가 아닌 문장의 "의미 단위"로 나누는 한 단계 진보한 방식입니다. 단락(Paragraph), 문장 맺음말(마침표), 구조적인 태그(Markdown Header 태그 등)를 기준으로 묶어냅니다.

> 📚 **이해를 돕는 예시:**
> 교과서를 복사할 때, 1페이지 절반 위치에서 내용(예: 조선시대 역사의 시작 부분)이 시작된다고 하더라도 기계적으로 500자씩 복사하는 게 아니라 "제1장 조선 전기"라는 제목에 맞춰서 통째로 하나의 묶음으로 파악하는 것입니다. 

머신러닝의 발전으로 아예 각 문장들의 '의미상 유사성'을 비교하여 뜻이 달라지는 스토리가 전환되는 구간을 찾아내 자동으로 청크를 분리하는 **고급 의미론적 모델** 기법도 널리 쓰이고 있습니다.

---

## 3. 문맥 엔지니어링 (Context Engineering)과 메타데이터 추가

조각조각 잘려진 청크 데이터는 그 자체로는 원본의 큰 맥락을 잃어버릴 위험이 있습니다. 청킹된 조각만 달랑 데이터베이스에 저장하는 것이 아니라, 문맥의 부가 정보(Metadata)를 같이 엮어주는 과정을 문맥 엔지니어링이라고 합니다. 

* **메타데이터(Metadata) 부착:** 각 청크에 출처 파일명, 작성자, 생성 날짜, 속한 챕터 이름 등을 태그로 달아줍니다.
* **요약 부착:** 문서를 부분 청킹하기 전, 문서 전체나 해당 장의 요약본을 미리 만들어 모든 청크 앞에 붙여줍니다. (이를 Summary-augmented chunking이라 합니다.)

> 🗂️ **이해를 돕는 예시:**
> 도서관의 책을 뜯어 낱장(Chunk)으로 보관한다고 해봅시다. 낱장만 보면 이 페이지가 무슨 책의 어느 내용인지 알 길이 없습니다. 그래서 낱장 맨 위 모서리 부분에 포스트잇(Metadata)으로 `[세종대왕 위인전 - 제 3장 한글 창제 - 24p]` 라고 적어두는 것과 같습니다. 나중에 "한글을 만든 사람"을 찾을 때 이 포스트잇 정보가 결정적인 식별 단서가 됩니다.

---

## 마무리하며

오늘은 데이터를 단순히 욱여넣지 않고 LLM이 소화 불량에 걸리지 않도록 맛있고 안전하게 잘라주는 청킹 기술에 대해 배웠습니다. 

다음 4주차에서는 이렇게 잘게 자른 텍스트 글자들을 어떻게 하면 컴퓨터가 숫자로 이해하고 서로 비슷한 내용끼리 묶어줄 수 있는지, RAG의 또 다른 핵심인 **임베딩(Embedding)** 과 **표현 학습(Representation Learning)** 에 대해 파헤쳐 보도록 하겠습니다.
