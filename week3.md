---
layout: default
title: 3주차. Advanced Document Chunking & Context Engineering
---

# 3주차: Advanced Document Chunking & Context Engineering (고급 문서 청킹과 문맥 엔지니어링)

RAG의 강력함은 방대한 양의 사내 데이터를 LLM과 융합하는 데 있습니다. 하지만 수백 페이지짜리 법률 위키피디아, 제품 매뉴얼을 통째로 LLM의 '컨텍스트 윈도우(Context Window)'에 던져버리면 모델이 다운되거나 정보 소화 불량 증세에 빠집니다.

이를 방지하기 위해 문서를 작고 일관된 의미를 가진 데이터 덩어리(Chunk)로 파내어 저장하는 **문서 청킹(Document Chunking)** 기법과, 이렇게 산산조각난 파편들이 정체성을 잃지 않게 메타데이터를 꼬리표로 묶어주는 **문맥 엔지니어링(Context Engineering)** 기술을 깊숙이 살펴봅니다.

---

## 1. 왜 청킹(Chunking)이 RAG 성능의 심장이자 병목인가?

모든 LLM은 처리할 수 있는 최대 토큰 한계(Token Limits)를 가지고 있습니다. 한계치를 극복하더라도 너무 많은 정보가 들어오면, 정작 중요한 정보가 가운데에 끼어 있을 때 이를 잃어버리는 'Lost in the Middle' 오류를 범합니다.

또한, 수만 자의 긴 문장을 통째로 임베딩하여 좌표 하나에 몰아넣어 버리면(Vector Transformation), 다양한 세부 기술 정보들이 다 짬뽕된 노이즈 좌표가 형성되어 정밀 검색이 영원히 실패하게 됩니다.

> 🍔 **이해를 돕는 예시: 커다란 수박 썰기**
> 커다란 수박(100페이지 짜리 문서)을 믹서기 입구(LLM)에 통째로 욱여넣을 수는 없습니다. 먹기 좋은 크기로 깍둑썰기를 해야만 들어갑니다. 하지만 수박을 무지성으로 막 눈감고 썰어서 껍질과 씨앗만 들어가는 조각이 생긴다면? 이는 맛이 없고 모델은 뱉어버릴 것입니다. 의미 결이 끊어지지 않고, 가장 조화로운 과육의 단위(문단 등)로 절단하는 기술이 바로 스마트 청킹입니다.

![Chunking Concept Analysis](assets/images_new/Fig_4_1_page_40.png)
*Fig 4.1: 글의 의미 단락과 토큰 제약 사이에서 청크가 나눠지는 컨셉 모형도*

---

## 2. 청킹의 세 가지 진화 단계 패러다임

### 2.1 Character/Token-based Chunking (고정 길이 기반)
가장 원시적이고 구현이 쉬운 기법입니다. 글자 수 500자 단위, 혹은 토큰 400개 단위로 무조건 칼로 자르듯 텍스트를 절단합니다.
* **치명적 단점:** 문장이나 심지어 단어가 중간에 절단되어(Truncation) 앞뒤 텍스트 간 의미의 연결고리가 하찮게 끊어지는 부작용이 막대합니다.

### 2.2 Overlapping Chunking (오버랩 꼬리 물기)
고정 길이 자르기의 문맥 훼손을 완화하기 위해, 이전 청크의 맨 끝 단어들 50자(Overlap length)를 다음 청크의 맨 앞머리에 중복으로 포함시켜 저장하는 방식입니다. 슬라이딩 윈도우처럼 작동합니다.

> 🔗 **이해를 돕는 예시: 파노라마 사진 촬영**
> 스마트폰으로 산 정상에서 파노라마 풍경을 찍을 때, 다음 장면 사진이 어긋나지 않게 도화지처럼 부드럽게 이어지도록 가장자리 전경을 조금 겹치게 구도를 잡습니다. 오버랩 역시 이음새 안전망 역할을 합니다.

### 2.3 Semantic Chunking & Structural Chunking (의미론/형태 구조적 청킹)
더 이상 단어 수를 세지 않습니다. NLP 모델을 사용하여 문장의 마침표, 줄바꿈 기호, 혹은 헤더 태그(`<h1>`, `<h2>`)나 마크다운 구조 챕터를 인지하여 철저하기 '의미 단위의 문단' 기준으로 청크를 유동성 있게 쪼갭니다. 테이블(표) 형식의 데이터는 가로세로를 인지하여 하나의 지식 단위 덩어리로 유지해킵니다.

![Table Parsing in Chunking](assets/images_new/Fig_4_1_15_page_102.png)
*Fig 4.1.15: 표(Table)나 다단계 리스트처럼 복잡도가 높은 구조를 RAG 문서 안료로 사용할 때 청킹이 망가지지 않게 캡슐화 파싱하는 구조.*

---

## 3. 문맥 엔지니어링: 파편화된 조각에 생명 불어넣기 (Context Enrichment)

산산이 조각난 수만 개의 문서 토막들은 자신이 1권의 책에서 비롯되었다는 거시적 정체성을 잃어버리는 일종의 '구조적 기억상실증'을 경험합니다. 이를 복원해 주는 것이 문맥 엔지니어링입니다.

1. **메타데이터 부착 (Metadata Tagging):** 
   쪼개진 각 청크 데이터에 이 데이터가 "어느 문서 파일에서 가져왔는지", "작성 일자는 언제인지", "어느 소제목 파트인지" 꼬리표를 달아 벡터 DB에 매핑합니다.
2. **요약 부착 기법 (Summary-Augmented / Parent-Child Chunking):** 
   각 작은 파편(자식 노드)들을 저장하기 전에, 해당 전체 상위 챕터의 요약본(부모 노드)을 미리 LLM을 통해 생성한 뒤, 작은 파편들 앞에 도장처럼 찍어 모두 덧붙여줍니다. 파편화된 조각만 봐도 숲 전체의 맥락을 가늠할 수 있어 검색 품질이 폭증합니다.

> 🗂️ **이해를 돕는 예시: 서적을 낱장으로 찢어 박스에 담기**
> 1만 권의 도서관 백과사전들을 찢어 박스에 담을 때, 그냥 종이(Chunk)만 두면 이 페이지가 무슨 책 내용인지 모릅니다. 매 낱장마다 포스트잇에 `[세종대왕 전집 - 제 3장 24p]` 라고 라벨링(Metadata)을 달고 `[조선시대 집현전 요약 내용]`을 써붙이는 치밀한 아카이빙 작업이 일어납니다.

---

## 🌟 [Deep Dive] 청킹 전략의 파괴적 혁신을 불러온 최신 리서치와 학술 동향

단순히 텍스트를 칼로 써는 방식을 넘어, 문맥을 입체적으로 유지하고 정보의 연속성을 보존하기 위한 글로벌 연구진들의 첨단 아키텍처 모델들을 소개합니다.

### 📜 1. RAPTOR: 문서를 나무처럼 요약하며 추상화하다
**[논문명]** *RAPTOR: Recursive Abstractive Processing for Tree-Organized Retrieval (Sarthi et al., 스탠포드 대학교, 2024)*
* **연구 배경:** "신데렐라 동화의 결말은 무어냐?" 같은 미시적인 질문은 텍스트 조각 검색으로 답할 수 있지만, "이 동화책 책 전체에서 관통하는 궁극적 철학과 주제 테마는 무엇인가?"와 같은 글로벌 요약 질문엔 RAG가 철저히 박살났습니다. (숲을 보지 못하고 나뭇잎 조각만 검색하기 때문)
* **해결 기술 (Architecture):** 
  긴 문서를 짧은 단말 청크(Leaf nodes)로 전부 쪼갠 뒤 끝내지 않습니다! 시스템이 인접한 청크들을 묶어, 하위 청크들을 아우르는 더 큰 부모 청크를 생성(의미론적 요약)합니다. 이를 나무구조(Tree)처럼 피라미드로 재귀적으로 계속 쌓아 올려 최종적으로는 책 전체 내용을 포괄하는 거대한 루트 요약 노드 청크 트리를 형성시킵니다.
* **의의:** 미세한 구체적 사실 확인은 잎사귀 청크 영역에서 캐치하고, 포괄적 테마 질문은 제일 꼭대기의 요약 청크에서 짚어내는, 즉 현미경과 망원경 시야를 동시에 모두 만족시키는 트리 구축 RAG 아키텍처의 찬란한 지평을 실현했습니다.

<div class="mermaid">
graph BT
    L1[문단 1 청크] --> P1(요약 노드 A)
    L2[문단 2 청크] --> P1
    L3[문단 3 청크] --> P2(요약 노드 B)
    L4[문단 4 청크] --> P2
    P1 --> Root{전체 통합 요약 루트 청크}
    P2 --> Root
    style Root fill:#f9d0c4,stroke:#e83e8c,stroke-width:2px
    style P1 fill:#d1ecf1
    style P2 fill:#d1ecf1
</div>

### 📜 2. Late Chunking: 먼저 온전히 뇌에 담고, 그 뒤에 잘라라
**[기술 리포트/트렌드]** *Late Chunking (Contextual Retrieval AI Research / Jina AI 등, 2024)*
* **연구 배경:** 일반적인 고전 RAG는 먼저 글을 조각조각 부수고(Chunk), 쪼개진 단위별로 임베딩 좌표점(Vector)을 부여했습니다. 이 과정에서 필시 앞뒤 문장이 잘리며 생기는 맹목적인 단절감, 문맥 이탈의 정보 누수가 엄청나게 스며들었습니다.
* **해결 기술 (Architecture):**
  획기적으로 순서를 리버스 역전시킵니다! 문서 수십 쪽 전체를 쪼개지 않고 단박에 한 번 통으로 트랜스포머 임베딩 모델에 먹여 깊이 투사(Encoding)시킵니다. 그러면 문서 전체의 앞뒤를 서로 거미줄처럼 비비며 참조한(Global Attention) 강력한 통맥락 토큰 텐서들이 형성됩니다. 그 직후! 임베딩이 끝난 깊은 장기 텐서 상태의 내부 공간 배열에서 기계 수학적으로 분절점 꼬리표를 찾아 뒤늦게 툭툭 잘라서 조각(Late cut) 내어 보관합니다.
* **의의:** 청크 덩어리 자체는 짧은 조각일지라도, 그 안의 DNA(벡터 배열)에는 이미 원본 책 전체의 거시적 맥락 냄새가 짙게 내포되어 들어있게 되는 기적적인 효과가 생깁니다!

<div class="mermaid">
flowchart LR
    subgraph 고전적 방식 (Early Chunking)
    Doc[전체 문서] --> C1[무지성 쪼개기 1] & C2[쪼개기 2]
    C1 --> V1[(문맥 잃은 벡터)]
    C2 --> V2[(문맥 잃은 벡터)]
    end
    
    subgraph Late Chunking 혁신
    Doc2[전체 문서 통채로] --> Embed[전체 통 임베딩 모델]
    Embed --> Tensor[Global Attention 텐서 배열]
    Tensor --> TV1[(맥락이 포함된 벡터 조각 1)] & TV2[(조각 2)]
    end
    
    style Doc2 fill:#fff3cd,stroke:#ffc107
    style Embed fill:#d4edda,stroke:#28a745
</div>

### 📜 3. Contextual Retrieval: 조각에 오리지널 맵을 입히다
**[논문명/보고서]** *Introducing Contextual Retrieval (Anthropic Research, 2024)*
* **연구 배경:** 앞서 배운 문맥 엔지니어링의 최종 진화형입니다. 단순한 파일명 꼬리표를 다는 수준에서는 쪼개진 문장의 모호함이 완벽히 해결되지 않습니다. (예: "그 회사의 재무 성과가 좋았다"라는 청크에서 '그 회사'가 누구인지 모름)
* **해결 기술 (Architecture):**
  본문 책 전체 내용을 뼈대로, 백그라운드 LLM이 각각의 잘려진 짧은 청크 파편들을 보며 **"이 청크 조각이 원본 문서에서 구체적으로 어떤 의미의 배경(Context)을 대변하고 있는지 완전한 문장으로 해석해 앞에 헤더로 붙여라"** 라고 지시하여 문맥 설명 메타데이터를 강제 영구 주입, 결찰시킵니다.
* **의의:** 잃어버린 대명사, 배경 지식이 완벽히 리커버리되어 임베딩 검색 엔진의 매칭률 성능 궤력을 상상 초월로 증강 폭발시켰습니다.

---

## 마무리하며

이번 3주차에는 무지막지한 텍스트 더미를 컴퓨터가 소화장애를 일으키지 않고 한땀한땀 이식할 수 있도록 하는 청킹과 컨텍스트의 기법들을 마스터했습니다.

이제 데이터 덩어리는 준비되었습니다. 다음 4주 차에서는 잘려진 이 알파벳 자연어 덩어리들을 기계만이 온전히 이해할 수 있는 좌표 체계(숫자)로 연금술을 시전하는 **Embedding Models (임베딩 모델)**과 이 좌표들의 코사인 유사도 측량에 대해 학습해 보겠습니다.
