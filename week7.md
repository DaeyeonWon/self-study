---
layout: default
title: 7주차. Knowledge Graph RAG & Graph-based Retrieval Systems
---

# 7주차: Knowledge Graph RAG & Graph-based Retrieval Systems (지식 그래프 RAG와 그래프 기반 검색 시스템)

지금까지 배운 벡터(Vector) 기반의 문서 검색 방식은 하나의 문서 덩어리 안에서 단서가 뭉쳐있을 때는 훌륭한 성능을 내지만, 여러 개의 문서에 흩어진 파편화된 정보들끼리 연결 고리(네트워크)를 추론해내는 작업에는 완전히 취약합니다. 

7주차에서는 최신 AI 학계와 산업계에서 가장 큰 주목을 받고 있는 **지식 그래프(Knowledge Graph)** 와 이를 결합한 **GraphRAG 시스템**에 대해 배웁니다.

---

## 1. 기존 벡터(Vector) RAG의 한계점: 멀티홉 추론 (Multi-hop Reasoning)

단순한 벡터 RAG 시스템은 직접적으로 유사한 문서들을 찾아주는 역할만 할 뿐, A 문서를 보고 B 단서를 얻고, B 문서에서 C 정답을 찾아내는 이른바 꼬리에 꼬리를 무는 **'멀티홉 추론(Multi-hop Reasoning)'** 에 취약합니다.

> 🧩 **이해를 돕는 예시: 전통적인 탐정과 어리숙한 도서관 사서**
> 사용자 질문: "대한민국 1대 대통령이 졸업한 대학교를 세운 설립자의 태어난 고향은 어디인가?"
> 
> * **일반 벡터 RAG:** "1대 대통령", "졸업", "대학교", "설립자", "고향" 등의 단어 의미 뭉치가 가리키는 파편화된 문서를 잔뜩 가져옵니다. "설립자의 고향"이라는 문서, "1대 대통령의 대학교"라는 문서를 별개로 가져오고 연결을 짓지 못해 엉뚱한 대답을 도출하기 일쑤입니다.
> 
> * **우리가 원하는 방식:** (1) 1대 대통령 문서를 찾음 -> "이승만" 추출 -> (2) 이승만이 졸업한 대학교 문서를 찾음 -> "조지워싱턴 대학교" 추출 -> (3) 조지워싱턴 대학의 설립자를 찾음 -> "루터 라이스" 추출 -> (4) 루터 라이스의 고향 문서를 찾음 -> "미국 매사추세츠" 답변.

이렇게 문서들 사이에 거미줄처럼 얽혀 있는 지점을 통과하려면 개체들 간의 '관계'를 별도로 매핑하는 기술이 필요합니다.

---

## 2. 지식 그래프 (Knowledge Graph) 란 무엇인가?

지식 그래프는 정보를 단순한 줄글 형태가 아니라, **정점(Node)** 과 점들을 잇는 **선과 관계망(Edge/Relation)** 으로 구조화하여 저장하는 데이터베이스 모델 시스템입니다. 

문서 군이 주어지면, LLM이 문장을 샅샅이 뒤져서 **Entity(주체, 객체)** 와 **Relation(그들 사이의 동사적 관계)** 을 추출해 수만 개의 점들이 이어진 거대한 마인드맵 지도를 그리는 원리입니다.

> 🕸️ **이해를 돕는 예시: 형사의 화이트보드 범죄 수사 지도**
> 영화에서 수사관이 화이트보드에 범죄자들의 사진(노드)을 압정으로 박고, 그들 사이를 빨간 실선(관계)으로 연결하는 장면을 떠올려보세요. 
> "해리포터 --- (재학 중) ---> 호그와트 --- (교장 선생님) ---> 덤블도어" 라는 선명한 트리 구조를 그립니다. 지식 그래프는 방대한 서류 뭉치를 이런 수학적 관계 지도로 미리 세팅해 두는 과정과 같습니다.

---

## 3. GraphRAG 시스템의 작동 원리

GraphRAG 시스템은 일반적인 텍스트 청킹-벡터 데이터베이스 아키텍처에, 지식 그래프를 탐색하는 구조를 혼합하여 설계됩니다. 사용자 질문이 들어오면 작동하는 로직은 다음과 같습니다.

1. **사용자 질문 분석:** 질문 속의 주어와 목적어, 즉 **주요 엔티티(Entity)** 를 추출해냅니다.
2. **그래프 진입점(Entry Point) 설정:** 앞서 구축해 둔 지식 그래프의 거미줄에서 질문에 나온 주요 개체의 '점(Node)'으로 진입합니다.
3. **그래프 순회 (Graph Traversing):** 진입점에서 시작해 연결된 선들을 2~3단계 이상 타고 넘어가며 연관된 이웃 개체 정보들(서브 그래프)을 싹쓸이하여 수집합니다.
4. **LLM 생성:** 이 연관 트리 정보 모드(서브 그래프 문맥)를 LLM에게 쥐여주어 대답을 구조적으로 종합하게 만듭니다.

> 🔦 **이해를 돕는 예시: 거미줄의 실타래 뽑기**
> 어두운 창고에서 물건을 찾을 때 플래시(일반 벡터 RAG)를 이리저리 비추며 눈동자만 굴려 찾는 대신, GraphRAG는 이미 창고의 모든 물건들이 줄로 이어져 있기 때문에 내가 찾는 밧줄의 한쪽 끝 단락(엔티티)만 꽉 붙잡고 쭉 잡아당기면 저 멀리 연결되어 보이지 않던 연관 지식 덩굴까지 한 번에 딸려오는 원리라고 보면 됩니다.

---

## 4. 언제 GraphRAG를 도입해야 할까?

모든 시스템에 지식 그래프라는 복잡한 기술을 세팅할 필요는 없습니다. 단순한 규정 문의나 메뉴얼 Q&A는 벡터 검색만으로도 충분합니다. 

하지만, 도메인 정보가 의학 체계망(증상-원인약품 상호작용), 법률(이전 판례들끼리의 인과율 연속성 연관 등), 복잡한 기업 조직도 시스템, 혹은 고객 행동 패턴 분석과 같은 분야라면, 단순히 문서의 문맥만을 읽지 않고 개체 간의 인과 지도를 그리는 GraphRAG가 압도적으로 정확한 성과를 발휘합니다.

## 마무리하며

이번 7주차를 통해 1차원적인 텍스트 문서 검색의 한계를 넘어서 인간의 뇌처럼 다차원적인 연결 구조와 맥락을 수학적으로 잡아내는 지식 그래프 기술에 대해 알아보았습니다.

대망의 마지막 8주차 강좌에서는, 구축한 이 수많은 RAG 파이프라인 시스템이 실제로 얼마나 똑똑하게 답변하는지를 기계적으로 수치화하고 채점하는 방법인 **RAG의 성능 평가 (Evaluation) 및 모니터링 체계** 에 대해 다루겠습니다.
