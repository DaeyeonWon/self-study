---
layout: default
title: 4주차. Embedding Models & Representation Learning for Retrieval
---

# 4주차: Embedding Models & Representation Learning for Retrieval (임베딩 모델과 검색을 위한 표현 학습)

지난 3주차에는 방대한 문서를 먹기 좋은 크기로 소분하는 청킹(Chunking)에 대해 배웠습니다. 하지만 컴퓨터는 텍스트(언어)를 우리처럼 직관적으로 이해하지 못합니다. 단지 0과 1의 숫자만을 알 뿐입니다. 그렇다면 컴퓨터는 어떻게 소분된 텍스트들의 '의미'를 파악해내어 검색할 수 있을까요? 

이번 주차에서는 텍스트를 컴퓨터가 이해할 수 있는 숫자의 좌표 체계로 변환하는 마법인 **임베딩(Embedding)** 과 **표현 학습(Representation Learning)** 에 대해 알아봅니다.

---

## 1. 텍스트 임베딩(Text Embedding)의 기본적인 개념

**임베딩(Embedding)** 이란 사람이 사용하는 단어나 문장을 특정 차원의 '숫자 배열(벡터, Vector)'로 변환하는 기술입니다. 즉, 의미를 수리적으로 "좌표 공간(Vector Space)"에 찍어주는 작업입니다. 

기존의 단순한 키워드 검색(전통적인 검색 방식)은 사용자가 "사과"라고 치면 문서에 "사과"라는 글자가 있는지 똑같이 생긴 단어만 찾았습니다. 반면 임베딩 모델을 활용하면 의미의 깊이를 파악할 수 있게 됩니다.

> 🗺️ **이해를 돕는 예시: 지도 위의 좌표계**
> 거대한 3차원 투명한 우주 공간(Vector Space)이 있다고 상상해 봅시다. 임베딩 모델은 이 우주 속에 단어들의 위치 좌표를 찍어 줍니다.
> 모델은 학습을 통해 서로 의미가 비슷하거나 같이 어울려 쓰이는 단어들은 서로 가까운 위치에 배치합니다.
> 예를 들어 [왕, 남자, 권력] 이 모인 지역과 [여왕, 여자, 권력] 이 모인 지역이 있다면, "왕진의사", "왕관" 등 모양만 비슷한 단어는 멀리 떨어지고, 대신 "국왕", "황제"는 글자 모양이 달라도 가까운 좌표에 위치하게 됩니다.

---

## 2. 검색에 특화된 표현 학습 (Representation Learning)

AI가 스스로 텍스트를 적절한 숫자 배열(좌표)로 매핑하는 규칙을 찾아내는 과정을 **표현 학습(Representation Learning)** 이라고 합니다. RAG 시스템에서 활용하는 임베딩 모델은 단순한 단어 임베딩을 넘어, '질문-답변' 맥락을 위한 고도의 훈련을 거칩니다.

### 차원(Dimension)이란 무엇인가?
현대의 딥러닝 임베딩 모델들은 의미를 표현하기 위해 3차원이 아니라, 768차원, 1536차원 등 수많은 차원의 숫자를 사용해 문장을 분해합니다. 각각의 차원은 사람이 쉽게 해석하기는 어렵지만, 성별, 시제, 긍정/부정, 주체의 성격 등 특정 '특성(Feature)'의 강도를 나타냅니다.

> 🎨 **이해를 돕는 예시: 와인 감별사 (특성 분해)**
> 와인을 평가할 때 단순히 "맛있다"가 아니라, (단맛, 신맛, 바디감, 타닌, 과일향, 오크향, 알콜도수) 등 수십 개의 기준 지표 점수표를 매깁니다.
> 표현 학습은 단어나 문장이 가진 보이지 않는 특성들을 와인 감별 수치처럼 쪼개어 수백, 수천 개의 점수 테이블(배열)로 나타내는 것입니다.

---

## 3. 임베딩 기반 검색원리: 코사인 유사도(Cosine Similarity) 산출

임베딩 모델을 통해 사용자의 질문과 문서(Chunk)들을 각각 좌표계 기반의 숫자 배열(Vector)로 바꿨다면, 이제 누가 나와 가장 비슷한지 어떻게 찾을까요?

가장 널리 쓰이는 수학적 비교 방식이 **코사인 유사도(Cosine Similarity)** 입니다. 원점에서 뻗어나가는 두 벡터(화살표) 간의 각도 코사인 값을 비교합니다. 각도가 좁을수록 유사도가 1에 가깝고(비슷한 의미), 각도가 멀어 벌어질수록 0 또는 -1(전혀 다른 의미)에 가까워집니다.

> 📐 **이해를 돕는 예시: 나침반의 방향**
> 사용자가 던진 질문 벡터가 "북동쪽"을 가리킨다고 가정해 봅니다.
> 데이터베이스 안에는 [정북쪽, 북북동쪽, 남쪽, 서쪽]을 가리키는 수많은 문서들의 벡터 화살표가 그려져 있습니다. 시스템은 화살표의 길이보다는 **"질문이 가리키는 방향(북동쪽)과 각도가 가장 일치하는 문서"**를 가장 관련성이 높은 답변 소스로 판단하여 불러오게(Retrieve) 됩니다. 

---

## 4. 좋은 임베딩 모델을 고를 때 고려사항

모든 임베딩 모델이 동일한 성능을 내지는 않습니다. RAG를 위해 어떤 것을 고를지 선택할 때는 다음과 같은 기준을 고려해야 합니다.

1. **도메인 특화:** 의료, 법률, 금융 데이터처럼 특수한 단어가 많은 경우 일반 (범용) 임베딩 모델은 좌표를 잘 찍지 못합니다. 해당 분야 논문 등으로 특화 훈련된 모델을 쓰면 성능이 폭발적으로 상승합니다.
   > **예시:** 일반인에게 "사과"는 과일이지만, 금융/기업 맥락에서 "Apple"은 회사입니다. 특성이 다르기 때문에 도메인에 맞는 모델의 시야가 필요합니다.
2. **다국어 (Multilingual) 지원:** 한국어로 묻고 영문 리포트의 결괏값을 가져오려면 다국어 혼합 의미 거리를 학습한 모델이 필수적입니다.
3. **차원의 크기:** 차원이 크면 더 미세한 의미를 담아 정밀도가 올라가지만(예: 1536차원), 그만큼 저장 공간과 속도를 희생하게 됩니다.

---

## 마무리하며

이번 4주차에서는 텍스트에 숨겨진 보이지 않는 의미를 숫자 값으로 변환(임베딩)하고, 이 숫자를 비교하여 유사한 정보의 방향을 잡아내는 RAG의 눈 역할을 하는 기술에 대해 배웠습니다. 

그런데 1,000개가 넘는 차원으로 이루어진 수백만 개의 문장 벡터(숫자 배열)들을 우리 피시판의 엑셀이나 일반 데이터베이스에 저장할 수 있을까요? 불가능합니다. 5주차에서는 이 어마어마한 숫자 무리들을 초고속으로 저장하고 검색할 수 있게 해주는 마법의 창고, **Vector Databases & Retrieval Architecture Design** 에 대해 다루어 보겠습니다.
