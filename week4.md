---
layout: default
title: 4주차. Embedding Models & Representation Learning for Retrieval
---

# 4주차: Embedding Models & Representation Learning for Retrieval (임베딩 모델과 검색을 위한 표현 학습)

지난 3주차에는 거대한 문서를 무리 없이 소화시키기 위해 의미 단위로 수만 등분하여 잘라내는 청킹(Chunking) 기술을 터득했습니다. 하지만 한 가지 중요한 허들이 남았습니다. 잘려진 건 여전히 우리가 읽는 자연어 '텍스트'이고, 컴퓨터는 '텍스트'를 직접 이해하지 못하며 오로지 숫자들의 행렬과 0, 1의 비트로만 사고합니다. 

그렇다면 데이터베이스는 문장의 속뜻을 어떻게 파악해서 질문과 매칭시킬 수 있을까요? 
4주차에서는 언어에 영혼(의미)을 덧입혀 수학 세계의 좌표 평면으로 변환하는 혁명적 기술인 **임베딩(Embedding)** 과 인공지능이 스스로 맥락을 잡아내는 **표현 학습(Representation Learning)** 을 배웁니다.

---

## 1. 텍스트 임베딩 (Text Embedding)의 작동 원리

**임베딩(Embedding)** 이란 단어들이나 통째로 된 문장 청크를 딥러닝 인코더(Encoder) 신경망에 통과시켜, 의미의 조각들을 특정 차원을 갖는 **'고정된 실수 배열 (벡터, Vector)'** 로 수학화시켜 투영(변환)해버리는 마법입니다. 즉, 의미를 "다차원 좌표 공간(Vector Space)"에 찍어주는 작업입니다.

전통적인 구형 키워드 검색 엔진은 질문 문구에 "사과"라는 글자 모양이 있는지 단순히 스캔하여 매칭했습니다. 하지만 임베딩 모델을 활용하면 의미의 깊이를 파악하게 되므로 "아이폰 회사"라는 말을 쳐도 "사과 로고의 스마트폰 다국적 기업"을 동일한 뜻(유사한 벡터 위치)으로 인식하게 됩니다.

![Embedding Representation Space](assets/images/page_22_img_1917.png)
*(PDF 자료 참고: 텍스트 의미들이 고차원 벡터로 군집화되어 차원 평면상에 분포된 모델 예시)*

> 🗺️ **이해를 돕는 예시: 지도 위의 좌표계 매핑**
> 거대한 3차원의 완벽히 투명한 우주 공간이 있다고 머릿속으로 상상해 봅시다. 그리고 당신은 우주를 설계하는 신입니다. 
> 임베딩 모델은 이 우주 공간 속에 단어들을 던져 구슬처럼 위치 좌표를 찍어 줍니다. "단어의 뜻이 비슷하거나 한 문장에 자주 같이 쓰이면 서로를 우주 공간 상에 가깝게 끌어당겨 놓아라"라는 학습 규칙을 수행합니다. 
> 예를 들어 [권력, 왕, 남자] 가 뭉친 은하계와 [권력, 여왕, 여자] 가 뭉친 지역이 생긴다면, "의사 가운" "왕관" 등 생긴 건 비슷하지만 쓰임새가 다른 건 저 멀리 배치됩니다. 

---

## 2. 표현 학습 (Representation Learning) 과 임베딩 차원 (Dimension)

RAG 분야에서 활용하는 임베딩 인코더 모델은 단순한 1차원적인 단어 분석이 아닌 '이 질문이 들어왔을 때 어떤 답변이 가장 연관도가 높은가?'를 유기적으로 맞추기 위해 무수히 많은 훈련 거칩니다. 이러한 AI 자가 규칙 도출 과정을 **표현 학습(Representation Learning)** 이라고 부릅니다.

### 임베딩의 구성 성분, 딥 차원(Dimension)
보통의 최신 임베딩 모델(오픈AI 사의 모델이나 오픈소스 모델 등)은 단순히 3차원 우주가 아닌 768차원, 1024차원, 무려 1536차원에 달하는 어마어마한 차원의 숫자 배열 데이터로 한 문장을 파헤쳐 분해합니다. (예: `[0.05, -0.32, 0.17 ... 1.05]`)

이 숫자의 의미를 사람이 하나하나 직관적으로 해석할 수는 없지만, 각각의 차원은 성별, 시제, 긍정/부정적 뉘앙스, 주체의 성격, 형식, 산업 카테고리 등 컴퓨터가 찾아낸 특정 '특성(Feature)'의 강약 점수를 의미합니다.

> 🎨 **이해를 돕는 예시: 소믈리에(와인 감별사)의 혓바닥**
> 와인을 평가할 때 일반인들은 단순히 "맛있네"로 끝내지만 (키워드 검색의 한계), 전문 소믈리에는 혓바닥으로 100가지 특성에 대해 점수표(벡터)를 매깁니다. 
> [단맛: 8, 신맛: 2, 묵직한 바디감: 9, 타닌: 7, 붉은 과일향: 5, 오크향: 10, 플로럴: 0 ...]
> 이 복잡한 평가 기준 지표 점수 100개가 모여 와인의 궁극적 정체성(Representation)을 나타냅니다. 임베딩의 768차원도 문장을 그와 똑같이 수학적으로 프로파일링 하는 과정입니다. 

---

## 3. 코사인 유사도(Cosine Similarity): 기계가 뜻을 매칭하는 법

청크 조각(문서들)들을 좌표계 위의 무수한 점들로 바꿔 저장했다 하고, 사용자가 "대한민국 자본주의의 역사" 라고 질문을 벡터 좌표(화살표)로 바꿨다면 이제 수만 개의 점 중 무엇이 가장 비슷한지 어떻게 찾을까요?

RAG 검색 엔진에서 가장 많이 쓰이는 거리/유사도 판별 공식이 바로 **코사인 유사도(Cosine Similarity)** 입니다. 
이는 우주의 중심 원점에서부터 각 문서 점들을 향해 뻗어나가는 두 화살표(벡터) 간의 **'각도 코사인(cos) 값'** 을 비교하는 방식입니다.

* 두 화살표의 뻗는 각도가 동일하다시피 좁아지면 유사도 점수가 1점(가장 비슷함)이 됩니다.
* 방향이 멀리 벌어져 코사인 값이 0점이 되거나 반대편(-1점)이 되면 전혀 연관이 없는 문서로 무시됩니다.

> 📐 **이해를 돕는 예시: 나침반의 바늘 방향 맞추기**
> 사용자가 던진 질문 벡터가 "북동쪽 45도" 방향 위를 찌르고 있다고 가정해 봅니다.
> 당신의 데이터베이스 안에는 정북쪽, 북북동쪽 50도, 남서쪽 등 수많은 문서 화살표가 그려져 있습니다. 
> 시스템은 문서 화살표가 아무리 길든 짧든 상관하지 않고 (내용이 길고 짧음을 무시함), **"나침반 자성에 가장 일치하는 각도로 휘어있는(가장 각도 크기가 비슷한 북북동쪽 50도 짜리)"** 문서들만을 골라서 RAG의 답변 재료로 꺼집어내 옵니다.

---

## 4. 완벽한 RAG를 위한 임베딩 모델 선택 가이드라인

수백 가지의 임베딩 모델 중 어떤 모델을 우리의 시스템 검색의 두뇌(Encoder)로 채택해야 할까요?

1. **지식/도메인 특화성:** 일반 상식과 대화에 특화된 범용 임베딩 모델은 의학, 반도체 회로, 특허법 등 심도 있는 전문 용어가 쏟아지는 영역에서는 좌표를 제대로 찍지 못합니다. 이 경우 파인튜닝된 특수 산업용(Domain-specific) 모델을 도입해야 성능이 폭발적으로 오릅니다.
2. **다국어 (Multilingual) 지원성:** 한국어 텍스트 문장으로 던진 질문이, 일본어와 영어로 된 기술 매뉴얼 문서와 동일한 뜻을 매칭하게 만들려면 의미론적인 교집합 거리를 완벽히 이해하는 다국어 혼합 모델이 필수불가결입니다.
3. **차원 크기와 운영 비용(Trade-off):** 차원이 크고 디테일하게 나눌수록 의미 감별의 정밀도는 극에 달하지만(예: 1536차원 이상의 대형 모델), 그 모든 방대한 숫자 배열을 데이터베이스에 저장하고 유지 보수하는 스토리지 비용과 메모리 로드 연산 시간은 증가합니다.

---

## 마무리하며

이번 주에는 의미 단위들로 산산조각 났던 자연어 글자 데이터를 숫자 화살표 더미 단위(벡터)로 치환해내어 인공지능이 영구적으로 보존할 수 있도록 의미의 우주에 찍는 과정을 탐구했습니다. 이제 컴퓨터는 어떤 모호한 질문이 들어와도 의미를 연산할 무기를 얻었습니다.

하지만 차원당 실수가 1,000개가 넘는 벡터 행렬 배열이 고작 수천 개가 아니라 수억 개 수십억 개로 확장된다면 우리가 알던 전통 엑셀이나 오라클 DB 같은 곳에서는 도저히 이 화살표 각도를 초 단위 안에 계산해서 찾아낼 재간이 없습니다. 

5주차에서는 세상에 나타난 놀라운 기술, 이러한 천문학적 벡터 행렬 계산과 검색을 실시간으로 감당하는 초대형 인프라 구조, **Vector Databases & Retrieval Architecture Design (벡터 DB 및 검색 아키텍트)** 에 대해 깊게 모험을 떠나 보겠습니다!
