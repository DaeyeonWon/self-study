---
layout: default
title: 1주차. RAG Fundamentals & System Challenges
---

# 1주차: RAG Fundamentals & System Challenges (RAG의 기초와 시스템적 과제)

환영합니다! 1주차 과정에서는 대규모 언어 모델(LLM)의 핵심 한계를 극복하기 위해 등장한 **RAG (Retrieval-Augmented Generation, 검색 증강 생성)** 의 기본 개념과 그 필요성을 배우고, 실제 엔터프라이즈 환경에서 직면하게 되는 주요 과제(System Challenges)들을 다방면으로 알아봅니다.

---

## 1. LLM의 한계와 RAG의 등장 배경

생성형 AI, 특히 대규모 언어 모델(LLM)은 방대한 데이터를 사전 학습(Pre-training)하여 사람처럼 자연스러운 문장을 만들어내는 데 탁월합니다. 하지만 모델이 커지고 발전했음에도 피할 수 없는 본질적인 한계점들이 존재합니다.

### 1) 환각 현상 (Hallucination)
LLM은 내부적으로 사실을 '기억'하는 것이 아니라 문맥상 "가장 그럴듯한 다음 단어의 확률"을 예측하여 생성합니다. 따라서 잘 모르는 질문을 받아도, 마치 아는 것처럼 확신에 차서 거짓말을 하는 현상인 환각이 발생합니다. 이는 고객 응대 서비스나 치명적인 보안이 필요한 엔터프라이즈 환경에서는 절대 용납될 수 없는 치명적인 결함입니다.

> 🍔 **이해를 돕는 예시:**
> 시험을 보는데 공부를 하지 않은 학생이 '모른다'고 빈칸으로 제출하는 대신, 질문에 나오는 그럴듯한 단어들을 조합해 어떻게든 답안지를 꽉 채워 제출하는 것과 같습니다. RAG는 이런 학생(LLM)에게 시험과 관련된 공식 '교과서(외부 문서)'를 시험 전에 쥐어주는 역할을 합니다.

### 2) 지식의 단절 (Knowledge Cut-off)
LLM은 수 개월 이상의 긴 사전 학습 과정을 거치기 때문에, 학습이 완료된 시점까지만의 지식만을 가집니다. GPT-4나 Claude가 훌륭하더라도 어제 발표된 최신 회사 규정이나 방금 일어난 뉴스 기사에는 전혀 대답할 수 없습니다. 계속 변하는 정보를 업데이트하기 위해 수백 억 원의 비용을 들여 매일 모델을 재학습하는 것은 불가능합니다.

### 3) 프라이버시 및 데이터 보안 문제
조직의 내부 문서, 기밀문서, 고객 데이터 등을 퍼블릭 LLM에 학습시키게 되면, 그 데이터가 모델의 가중치 어딘가에 녹아내려 결국 제3자에게 누출될 위험이 존재합니다 (Data leak). 

---

## 2. RAG(검색 증강 생성)의 해결 방식과 아키텍처

RAG 기술은 지식을 LLM 자체 신경망 안에 우겨넣는 대신, 지식을 '데이터베이스'로 분리하고 이를 기반으로 LLM이 추론만 담당하도록 아키텍처를 이원화(Separation of concerns)합니다.

![RAG Basic Architecture](assets/images/page_5_img_294.png)
*(PDF 자료 참고 이미지)*

### 이상적인 RAG 파이프라인 구조
1. **데이터 수집 및 매핑 (Ingestion / Indexing):** 
   사내의 다양한 데이터(PDF, 워드, 데이터베이스 등)를 텍스트 덩어리(Chunk)로 나눈 뒤 임베딩 모델을 통해 숫자 벡터(디지털 좌표)로 변환해 Vector Database에 안전하게 보관합니다.
2. **질문 검색 (Retrieval):** 
   사용자가 "최신 연차 규정이 어떻게 돼?"라고 묻습니다. RAG 시스템은 질문을 동일하게 수치 배열로 변환한 뒤, Vector Database에서 '이와 가장 의미가 비슷한 지식' 5개를 초점 탐색해 꺼내옵니다.
3. **생성 및 증강 (Augmented Generation):** 
   꺼내온 외부 지식 문장들(Context)과 원래 사용자 질문(Query)을 묶어, LLM에게 전달합니다. "아래의 문맥(Context)만을 기반으로 대답해줘"라고 지시하면, 환각 없이 완벽한 대답이 나옵니다.

---

## 3. RAG vs 파인튜닝 (Fine-Tuning) vs 프롬프트 엔지니어링

엔터프라이즈 서비스를 기획할 때 가장 많이 헷갈리는 3가지 방식의 차이점을 명확히 짚어보겠습니다.

* **프롬프트 엔지니어링 (Prompt Engineering):** 
  별도의 시스템 엔지니어링 없이 모델과 대화하는 규칙만 조정하는 방식입니다. 문체를 친절하게 바꾸거나 출력 형태만 바꾸고 싶을 때 매우 유용하지만, '새로운 팩트(Fact)'를 주입해줄 수는 없습니다.
* **파인튜닝 (Fine-Tuning):** 
  LLM의 내부 뇌 구조 가중치 백만 개를 살짝 업데이트하여 특정 도메인(의학 어투, 법률 문체 등)에 모델을 완전히 길들이는 방식입니다. 그러나 파인튜닝 또한 특정 사실을 완벽히 암기하는 데에는 약하며 수시로 변하는 데이터를 대응할 수 없습니다.
* **RAG (Retrieval-Augmented Generation):** 
  지식(팩트) 업데이트와 완벽한 소스 출처(Citation) 표기가 가장 큰 장점입니다. "참고 문헌"을 실시간으로 검색해 붙여주므로 환각을 방지하고 보안성을 유지하며 비용 효율이 가장 뛰어납니다. 

---

## 4. 실제 RAG 구축 시 마주하는 시스템 과제 (System Challenges)

RAG의 이론은 무척 아름답지만, 실제 코드 작성을 통해 구축해 보면 수많은 문제점이 터져 나옵니다. 이번 스터디에서 8주 차 동안 하나씩 박살내야 할 7단계 과제들입니다.

![Challenges Chart](assets/images/page_3_img_236.png)
*(PDF 자료 참고: RAG 파이프라인의 에러 발생 지점)*

1. **Missing Content (문서 자체가 검색 DB에 없음):** 
   애초에 저장되지 않은 지식을 물을 때 RAG가 무리하게 대답을 내려는 현상입니다. 
   > 💡 **예시:** 도서관에 없는 책 내용을 물어봤는데, 사서가 어떻게든 짐작해서 지어내는 현상입니다. (해결책: 철저한 프롬프트 통제 필요)
2. **Missed Top Ranked Documents (핵심 문서가 검색되지 않음):** 
   검색 엔지니어링의 한계로 정답 문서를 빼먹고 쓸데없는 문서를 LLM에게 전달하는 현상입니다.
3. **Not in Context (가운데에서 길을 잃음, Lost in the Middle):** 
   최근 논문에서 밝혀진 바와 같이 LLM에게 20쪽짜리 문서(Context)를 줘버리면, 문맥 중간에 있는 중요한 단서를 놓치는 확률이 급격히 증가합니다.
   > 💡 **예시:** 인간도 너무 긴 메일을 받으면 첫 줄과 끝 줄만 읽고 중간 내용은 스킵하는 것과 똑같습니다. 문맥의 다이어트(최적화)가 중요합니다.
4. **Incorrect Specificity (추출 구체성 실패):** 
   찾아온 문서는 훌륭한데 LLM이 요약 요점을 엉뚱하게 잡거나 너무 뭉뚱그려 대답합니다.
5. **Wrong Format (형식 파괴):** 
   JSON, CSV 등 원하는 시스템 포맷을 지키지 못합니다.

---

## 마무리하며
1주 차 강좌에서는 RAG라는 거대한 산을 등반하기 위한 지도를 펼쳐보았습니다. LLM의 환각과 지식 단절이라는 치명적 약점을 RAG 아키텍처가 어떻게 보완하는지, 그리고 왜 단순 튜닝만으로는 안 되는지 확고히 이해하셨을 것입니다.

2주 차에서는 이러한 과제 중 첫 번째 단계인 **'환각을 강제로 누르고 LLM을 통제하는 기법'**, 바로 **Prompting Strategies (프롬프팅 전략)** 에 대해 훨씬 더 구체적인 테크닉을 다루어 보겠습니다.
