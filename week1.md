---
layout: default
title: 1주차. RAG Fundamentals & System Challenges
---

# 1주차: RAG Fundamentals & System Challenges (RAG의 기초와 시스템적 과제)

환영합니다! 1주차 과정에서는 대규모 언어 모델(LLM)의 핵심 한계를 극복하기 위해 등장한 **RAG (Retrieval-Augmented Generation, 검색 증강 생성)** 의 기본 개념과 그 필요성을 배우고, 실제 시스템을 구축할 때 직면하게 되는 주요 과제(System Challenges)들을 알아봅니다.

---

## 1. LLM의 한계와 RAG의 등장 배경

대규모 언어 모델(LLM)은 방대한 데이터를 학습하여 사람처럼 자연스러운 문장을 만들어내는 데 탁월합니다. 하지만 완벽하지는 않으며, 다음과 같은 치명적인 한계들을 가지고 있습니다.

### 1) 환각 현상 (Hallucination)
LLM은 사실 여부를 떠나 문맥상 "가장 그럴듯한 다음 단어"를 예측하여 문장을 생성합니다. 잘 모르는 질문을 받아도, 마치 아는 것처럼 당당하게 거짓말을 하는 현상을 '환각(Hallucination)'이라고 부릅니다. 

연구에 따르면, 환각은 크게 다음과 같은 세 가지 형태로 나뉩니다.
* **입력 충돌 (Input-Conflicting):** 사용자가 제공한 입력이나 의도와 반대되는 내용을 생성하는 경우 (예: 저녁 식사 레시피를 물었는데 점심 레시피를 알려줌).
* **문맥 충돌 (Context-Conflicting):** 직전 문맥에서 전혀 언급되지 않은 내용을 갑자기 만들어내는 경우.
* **사실 충돌 (Fact-Conflicting):** 실제 사실과 명백히 다른 내용을 출력하는 경우 (예: 토마토에 칼슘이 가장 많다고 진술함).

### 2) 지식의 단절 (Knowledge Cut-off)
LLM은 학습이 완료된 시점(Training cut-off)까지만의 지식을 가지고 있습니다. 어제 일어난 뉴스나 오늘 아침 변경된 사내 규정에 대해서는 대답할 수 없습니다.

### 3) 프라이버시 및 데이터 보안 한계
기업의 민감한 데이터나 개인 정보를 퍼블릭 LLM에 바로 학습시키는 것은 심각한 보안 침해를 야기할 수 있습니다. 

<br/>

## 2. RAG(검색 증강 생성)란 무엇인가?

위와 같은 한계를 극복하기 위해 제안된 것이 바로 **RAG (Retrieval-Augmented Generation)** 입니다. 말 그대로 정보를 **검색(Retrieval)** 하여, 모델의 텍스트 **생성(Generation)** 능력을 **증강(Augmented)** 시키는 기술입니다.

LLM이 모든 것을 암기한 채 시험을 보는 학생이라면, RAG는 **오픈북 테스트(Open-book Test)** 를 치르는 학생과 같습니다. 모르는 것을 무작정 지어내지 않고, 관련된 신뢰할 수 있는 외부 문서(책, 매뉴얼 등)를 먼저 찾아보고 그 내용을 바탕으로 답변을 작성합니다.

### 💡 RAG 시스템의 핵심 구조 (3단계)

1. **인코딩 프로세스 (Data Indexing):** 
   책이나 방대한 문서를 컴퓨터가 이해하기 쉬운 숫자의 배열(Embedding Vector)로 변환해 저장소(Vector Database)에 보관해 둡니다.
2. **검색 단계 (Retrieval):** 
   사용자가 질문을 던지면, 1번에서 저장해둔 방대한 지식 아카이브 중에서 질문과 가장 의미가 유사한(관련성 높은) 문서를 Top-K개(예: 3개~5개) 추출해냅니다.
3. **생성 단계 (Generation):** 
   검색된 "정확한 외부 지식 문서"와 "사용자의 질문"을 묶어서 LLM에게 전달합니다. 모델은 이 외부 지식을 기반으로 최종 답변을 말끔하게 생성합니다. 이로써 출처 기반의 정확하고 환각 없는 답변이 탄생합니다.

---

## 3. RAG vs. 파인튜닝 (Fine-Tuning) vs. 프롬프트 엔지니어링

많은 분들이 "우리 회사 데이터를 학습시키려면 모델을 재학습(파인튜닝) 해야 하는 것 아닌가요?" 라고 묻습니다. 세 가지 접근법의 차이를 명확히 구분하는 것이 중요합니다.

* **프롬프트 엔지니어링 (Prompt Engineering):** 외부 데이터 연동이나 학습 없이, 질문하는 방식을 교정하여 LLM이 답변의 형태나 규칙을 잘 따르도록 조정하는 가장 가벼운 기법입니다.
* **파인튜닝 (Fine-Tuning):** LLM 자체의 파라미터(신경망 가중치)를 업데이트하여 모델의 어투를 바꾸거나 특정 도메인의 작업 성능을 비약적으로 높이는 기법입니다. 하지만 새로운 지식을 계속 업데이트 하기에 비용이 매우 크고, 환각을 완벽히 잡지 못합니다.
* **RAG (검색 증강 생성):** LLM 자체를 건드리지 않고, 외부 지식 기반을 "참고 문헌"처럼 연동하여 붙여주는 방식입니다. 데이터가 자주 변하고 추가 비용 없이 즉각적인 지식 업데이트가 필요한 상황에 적합합니다.

> **결론:** 지식(Fact) 업데이트와 환각 방지가 목적이라면 RAG가 최우선 선택지입니다. 문체 변경이나 규칙 준수가 목적이라면 파인튜닝을 적용하거나 RAG와 파인튜닝을 병행하는 것이 이상적입니다.

---

## 4. RAG 시스템 설계 시 직면하는 주요 과제 (System Challenges)

이제 RAG 시스템을 실제 서비스 환경(Enterprise-grade)에 도입하려 할 때 겪는 7가지 전형적인 실패 포인트와 과제들을 살펴봅시다.

1. **Missing Content (콘텐츠 유실):** 
   사용자가 물어본 질문에 대한 정답이 애초에 외부 데이터베이스 안에 존재하지 않는 경우입니다. 이 경우 RAG 시스템은 무리해서 답변을 생성하려다 되려 환각을 일으킬 수 있습니다.
2. **Missed Top Ranked Documents (핵심 문서 누락):** 
   정답이 문서 군에 존재하지만 반대로 검색(Retrieval) 과정에서 제대로 찾아내지 못해 LLM에게 전달되지 못한 경우입니다.
3. **Not in Context (문맥 초과 또는 중요도 희석):** 
   문서를 찾아서 LLM에게 넘겨줬는데, 너무 많은 문서를 한 번에 넘겨줘서 정작 중요한 내용이 LLM의 집중력을 흐리게 만들어 무시된 경우입니다 (Lost in the middle 현상 등).
4. **Not Extracted (추출 실패):** 
   찾아준 문서 안에 정답이 있었지만 LLM이 그 핵심 내용을 잡아내서 문장으로 승화시키지 못한 경우.
5. **Wrong Format (형식 위반):** 
   사용자가 "표 형식으로 요약해줘"라고 했지만, LLM이 프롬프트를 무시하고 줄글로 대답해 버리는 경우.
6. **Incorrect Specificity (부적절한 구체성):** 
   사용자는 아주 구체적인 "실행 방안"을 물었는데, 모델은 너무 모호하고 광범위한 이론적 답변만 한 경우(또는 그 반대).
7. **Incomplete (불완전한 답변):** 
   답변의 내용이 맞긴 하지만, 일부만 말해주고 답변이 툭 끊기거나 포괄적인 내용이 생략된 경우입니다.

이러한 과제들을 해결하기 위해서는, 앞으로 2주차부터 배우게 될 **고급 청킹, 정교한 임베딩 모델 선택, 프롬프트 튜닝 기법, 그리고 리랭커(Reranker)** 등의 추가적인 요소들이 반드시 필요합니다.

---

## 마무리하며

이번 주차에서는 RAG가 왜 필요하며 어떤 기본 구조(인덱싱-검색-생성)로 이루어져 있는지, 실제 구축 시 어떤 도전 과제가 발생하는지 이론적인 기반을 다졌습니다. 2주차에서는 이러한 한계를 줄이기 위해 LLM에 적용할 수 있는 **'환각을 줄이기 위한 고도화된 프롬프팅 전략'**에 대해 본격적으로 깊이 파고들 예정입니다.
